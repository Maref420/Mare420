{"b51cd1c07c8b72071e41df6d": {"response": "import collections\nfrom typing import Deque, Generic, Iterable, Iterator, Optional, TypeVar\n\nT = TypeVar(\"T\")\n\n\nclass QueueEmptyError(IndexError):\n    \"\"\"Raised when attempting to dequeue or peek from an empty Queue.\"\"\"\n\n\nclass Queue(Generic[T]):\n    \"\"\"\n    A first-in, first-out (FIFO) queue implementation.\n\n    Parameters\n    ----------\n    initial_items : Iterable[T], optional\n        An iterable of items to initialize the queue with. Defaults to an empty queue.\n\n    Examples\n    --------\n    >>> q = Queue[int]()\n    >>> q.enqueue(1)\n    >>> q.enqueue(2)\n    >>> q.dequeue()\n    1\n    >>> q.peek()\n    2\n    >>> q.is_empty()\n    False\n    \"\"\"\n\n    __slots__ = (\"_items\",)\n\n    def __init__(self, initial_items: Optional[Iterable[T]] = None) -> None:\n        self._items: Deque[T] = collections.deque()\n        if initial_items is not None:\n            for item in initial_items:\n                self.enqueue(item)\n\n    def enqueue(self, item: T) -> None:\n        \"\"\"\n        Add an item to the end of the queue.\n\n        Parameters\n        ----------\n        item : T\n            The item to add.\n\n        Returns\n        -------\n        None\n        \"\"\"\n        self._items.append(item)\n\n    def dequeue(self) -> T:\n        \"\"\"\n        Remove and return the item at the front of the queue.\n\n        Raises\n        ------\n        QueueEmptyError\n            If the queue is empty.\n\n        Returns\n        -------\n        T\n            The dequeued item.\n        \"\"\"\n        if not self._items:\n            raise QueueEmptyError(\"Cannot dequeue from an empty queue.\")\n        return self._items.popleft()\n\n    def peek(self) -> T:\n        \"\"\"\n        Return the item at the front of the queue without removing it.\n\n        Raises\n        ------\n        QueueEmptyError\n            If the queue is empty.\n\n        Returns\n        -------\n        T\n            The item at the front of the queue.\n        \"\"\"\n        if not self._items:\n            raise QueueEmptyError(\"Cannot peek into an empty queue.\")\n        return self._items[0]\n\n    def is_empty(self) -> bool:\n        \"\"\"\n        Check whether the queue is empty.\n\n        Returns\n        -------\n        bool\n            True if the queue has no items, False otherwise.\n        \"\"\"\n        return not self._items\n\n    def __len__(self) -> int:\n        \"\"\"Return the number of items in the queue.\"\"\"\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[T]:\n        \"\"\"Return an iterator over the queue's items.\"\"\"\n        return iter(self._items)\n\n    def __repr__(self) -> str:\n        items = \", \".join(repr(item) for item in self._items)\n        return f\"{self.__class__.__name__}([{items}])\"\n\n__all__ = [\"Queue\", \"QueueEmptyError\"]", "ts": 1788519431.5174313, "model": "qwen/qwen3.8-27b", "hits": 0, "len": 2717}, "2e77c36074ffc0b122a6208e": {"response": "\"\"\"\nModule: multiply\nProject: final_test\nArchitecture: math\n\nProvides secure, production-grade multiplication operations.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom decimal import Decimal, InvalidOperation\nfrom typing import Union\n\nNumber = Union[int, Decimal]\n\n\ndef multiply(a: Number, b: Number) -> Decimal:\n    \"\"\"\n    Multiply two numbers with strict type validation and precision control.\n\n    This function performs multiplication using Decimal to ensure exact\n    arithmetic, suitable for financial calculations where floating-point\n    inaccuracies are unacceptable.\n\n    Args:\n        a: The first operand. Must be an int or Decimal.\n        b: The second operand. Must be an int or Decimal.\n\n    Returns:\n        Decimal: The exact product of a and b.\n\n    Raises:\n        TypeError: If either argument is not an int or Decimal.\n        ValueError: If either argument is not a finite number.\n    \"\"\"\n    # Validate types\n    if not isinstance(a, (int, Decimal)):\n        raise TypeError(\n            f\"Argument 'a' must be of type int or Decimal, got {type(a).__name__}\"\n        )\n    if not isinstance(b, (int, Decimal)):\n        raise TypeError(\n            f\"Argument 'b' must be of type int or Decimal, got {type(b).__name__}\"\n        )\n\n    # Convert int to Decimal for consistent arithmetic\n    if isinstance(a, int):\n        a = Decimal(a)\n    if isinstance(b, int):\n        b = Decimal(b)\n\n    # Validate finiteness\n    if not a.is_finite():\n        raise ValueError(\"Argument 'a' must be a finite number\")\n    if not b.is_finite():\n        raise ValueError(\"Argument 'b' must be a finite number\")\n\n    # Perform multiplication\n    result = a * b\n\n    # Ensure result is finite (should always be true for finite inputs, but defensive)\n    if not result.is_finite():\n        raise ArithmeticError(\"Multiplication resulted in a non-finite value\")\n\n    return result", "ts": 1788519936.0529656, "model": "qwen/qwen3.8-27b", "hits": 0, "len": 1884}, "5bc5db87d0df227eb3f88598": {"response": "\"\"\"\nlinkedlist.py\n\nA production-grade, type-hinted implementation of a singly linked list.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any, Generic, Iterator, Optional, TypeVar\n\nT = TypeVar(\"T\")\n\n\nclass Node(Generic[T]):\n    \"\"\"\n    Represents a single node in the linked list.\n\n    Attributes:\n        data (T): The data stored in the node.\n        next (Optional[Node[T]]): A reference to the next node in the list.\n    \"\"\"\n\n    __slots__ = (\"data\", \"next\")\n\n    def __init__(self, data: T) -> None:\n        \"\"\"\n        Initialize a new Node.\n\n        Args:\n            data (T): The data to store in the node.\n        \"\"\"\n        self.data: T = data\n        self.next: Optional[Node[T]] = None\n\n    def __repr__(self) -> str:\n        \"\"\"\n        Return a string representation of the node.\n\n        Returns:\n            str: A string representation of the node's data.\n        \"\"\"\n        return f\"Node(data={self.data!r})\"\n\n\nclass LinkedList(Generic[T]):\n    \"\"\"\n    A singly linked list data structure.\n\n    This class provides methods to append, prepend, delete, and search for\n    elements within the list. It maintains a head pointer and a count of\n    elements for O(1) length retrieval.\n\n    Type Parameters:\n        T: The type of elements stored in the list.\n    \"\"\"\n\n    def __init__(self) -> None:\n        \"\"\"\n        Initialize an empty linked list.\n        \"\"\"\n        self._head: Optional[Node[T]] = None\n        self._tail: Optional[Node[T]] = None\n        self._size: int = 0\n\n    def __len__(self) -> int:\n        \"\"\"\n        Return the number of elements in the list.\n\n        Returns:\n            int: The number of elements in the list.\n        \"\"\"\n        return self._size\n\n    def __bool__(self) -> bool:\n        \"\"\"\n        Return True if the list is not empty.\n\n        Returns:\n            bool: True if the list contains elements, False otherwise.\n        \"\"\"\n        return self._size > 0\n\n    def __iter__(self) -> Iterator[T]:\n        \"\"\"\n        Return an iterator over the elements in the list.\n\n        Yields:\n            T: The data from each node in the list.\n        \"\"\"\n        current: Optional[Node[T]] = self._head\n        while current is not None:\n            yield current.data\n            current = current.next\n\n    def __repr__(self) -> str:\n        \"\"\"\n        Return a string representation of the linked list.\n\n        Returns:\n            str: A string representation of the list contents.\n        \"\"\"\n        items = [repr(item) for item in self]\n        return f\"LinkedList([{', '.join(items)}])\"\n\n    def append(self, data: T) -> None:\n        \"\"\"\n        Append a new element to the end of the list.\n\n        Args:\n            data (T): The data to append.\n        \"\"\"\n        new_node = Node(data)\n        if self._tail is None:\n            self._head = new_node\n            self._tail = new_node\n        else:\n            self._tail.next = new_node\n            self._tail = new_node\n        self._size += 1\n\n    def prepend(self, data: T) -> None:\n        \"\"\"\n        Prepend a new element to the beginning of the list.\n\n        Args:\n            data (T): The data to prepend.\n        \"\"\"\n        new_node = Node(data)\n        new_node.next = self._head\n        self._head = new_node\n        if self._tail is None:\n            self._tail = new_node\n        self._size += 1\n\n    def delete(self, data: T) -> bool:\n        \"\"\"\n        Delete the first occurrence of the specified data from the list.\n\n        Args:\n            data (T): The data to delete.\n\n        Returns:\n            bool: True if the element was found and deleted, False otherwise.\n        \"\"\"\n        if self._head is None:\n            return False\n\n        # Check if the head node contains the data\n        if self._head.data == data:\n            self._head = self._head.next\n            if self._head is None:\n                self._tail = None\n            self._size -= 1\n            return True\n\n        # Traverse the list to find the node to delete\n        current: Optional[Node[T]] = self._head\n        while current.next is not None:\n            if current.next.data == data:\n                current.next = current.next.next\n                # Update tail if the deleted node was the tail\n                if current.next is None:\n                    self._tail = current\n                self._size -= 1\n                return True\n            current = current.next\n\n        return False\n\n    def search(self, data: T) -> bool:\n        \"\"\"\n        Search for an element in the list.\n\n        Args:\n            data (T): The data to search for.\n\n        Returns:\n            bool: True if the element is found, False otherwise.\n        \"\"\"\n        current: Optional[Node[T]] = self._head\n        while current is not None:\n            if current.data == data:\n                return True\n            current = current.next\n        return False\n\n    def clear(self) -> None:\n        \"\"\"\n        Remove all elements from the list.\n        \"\"\"\n        self._head = None\n        self._tail = None\n        self._size = 0", "ts": 1788520226.6704638, "model": "qwen/qwen3.8-27b", "hits": 0, "len": 5069}, "e9bdeb153670bc6b4cfdc367": {"response": "\"\"\"\nModule: fibonacci\nProject: deploy_test\nArchitecture: math\n\nProvides a production-grade implementation of the Fibonacci sequence.\n\"\"\"\n\nfrom __future__ import annotations\n\n\ndef fibonacci(n: int) -> int:\n    \"\"\"\n    Calculate the n-th Fibonacci number.\n\n    The Fibonacci sequence is defined as:\n        F(0) = 0\n        F(1) = 1\n        F(n) = F(n-1) + F(n-2) for n > 1\n\n    This implementation uses an iterative approach with O(n) time complexity\n    and O(1) space complexity, ensuring efficiency and avoiding recursion\n    depth limits.\n\n    Args:\n        n (int): The index of the Fibonacci number to compute. Must be a\n                 non-negative integer.\n\n    Returns:\n        int: The n-th Fibonacci number.\n\n    Raises:\n        TypeError: If 'n' is not an integer.\n        ValueError: If 'n' is negative.\n\n    Examples:\n        >>> fibonacci(0)\n        0\n        >>> fibonacci(1)\n        1\n        >>> fibonacci(10)\n        55\n    \"\"\"\n    if not isinstance(n, int):\n        raise TypeError(f\"Input must be an integer, got {type(n).__name__}\")\n    if n < 0:\n        raise ValueError(f\"Input must be non-negative, got {n}\")\n\n    if n == 0:\n        return 0\n    if n == 1:\n        return 1\n\n    prev, curr = 0, 1\n    for _ in range(2, n + 1):\n        prev, curr = curr, prev + curr\n\n    return curr\n\n\nif __name__ == \"__main__\":\n    # Basic self-test for verification\n    assert fibonacci(0) == 0\n    assert fibonacci(1) == 1\n    assert fibonacci(10) == 55\n    assert fibonacci(20) == 6765\n\n    try:\n        fibonacci(-1)\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"Expected ValueError for negative input\")\n\n    try:\n        fibonacci(1.5)\n    except TypeError:\n        pass\n    else:\n        raise AssertionError(\"Expected TypeError for non-integer input\")\n\n    print(\"All tests passed.\")", "ts": 1788521569.402365, "model": "qwen/qwen3.8-27b", "hits": 0, "len": 1829}, "1b69ca7ebbc9dc589b7b891d": {"response": "\"\"\"\nModule: fibonacci\nProject: deploy_full_test\nDescription: Provides a secure and efficient implementation of the Fibonacci sequence.\n\"\"\"\n\nfrom typing import Union\n\n\ndef fibonacci(n: int) -> int:\n    \"\"\"\n    Calculate the n-th Fibonacci number.\n\n    This function computes the n-th number in the Fibonacci sequence using an\n    iterative approach for optimal time and space complexity (O(n) time, O(1) space).\n    The sequence is defined as:\n        F(0) = 0\n        F(1) = 1\n        F(n) = F(n-1) + F(n-2) for n > 1\n\n    Args:\n        n (int): The index of the Fibonacci number to compute. Must be a non-negative integer.\n\n    Returns:\n        int: The n-th Fibonacci number.\n\n    Raises:\n        TypeError: If 'n' is not an integer.\n        ValueError: If 'n' is negative.\n    \"\"\"\n    if not isinstance(n, int):\n        raise TypeError(f\"Input must be an integer, got {type(n).__name__}\")\n    \n    if n < 0:\n        raise ValueError(\"Input must be a non-negative integer\")\n    \n    if n == 0:\n        return 0\n    if n == 1:\n        return 1\n    \n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    \n    return b", "ts": 1788521659.43767, "model": "qwen/qwen3.8-27b", "hits": 0, "len": 1137}}