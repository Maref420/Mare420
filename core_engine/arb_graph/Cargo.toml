[package]
name = "arb_graph"
version = "0.1.0"
edition = "2021"
description = "Weighted directed graph with negative cycle detection for Atlas-AI"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
