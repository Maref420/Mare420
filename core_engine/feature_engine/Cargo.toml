[package]
name = "feature_engine"
version = "0.1.0"
edition = "2021"
description = "Microstructure feature computation engine for Atlas-AI DaaS"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
