[package]
name = "memory_store"
version = "0.1.0"
edition = "2021"
description = "Governed memory store for Atlas-AI agents per ADR-006"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
